import running_money from '../VueComponents/running_mongy_comment'
const runingMoneyCard = {
  install (Vue, connection, opts) {
    Vue.component('runingMoney',running_money)
  }
}
export default runingMoneyCard
